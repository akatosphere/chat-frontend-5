import { createIcon } from 'shared/ui/icon';
import Clear from './clear.svg';
import Search from './search.svg';

export const SearchIcon = createIcon(Search);
export const ClearIcon = createIcon(Clear);
